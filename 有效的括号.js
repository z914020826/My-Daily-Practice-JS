function isValid(str) {
  const stack = [];
  //   if (s.length % 2 !== 0) return false;

  for (const s in str) {
    console.log(s);
  }
}
isValid("()");
console.log();

// 遍历字符串
// 1. for...in 遍历字符串
// 2. for...of 遍历字符串
// 3. for 遍历字符串
// 4. forEach 遍历字符串
// 5. map 遍历字符串
// 6. filter 遍历字符串
// 7. reduce 遍历字符串
// 8. every 遍历字符串
